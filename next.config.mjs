/** @type {import('next').NextConfig} */
const nextConfig = {
   //images configration
    images: {
        domains: ['images.unsplash.com', 'res.cloudinary.com']
    }
};

export default nextConfig;